{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "▷ ローカルプレイ開始",
      "detail": "make local && make local-portal — starts the scoring API, then the browser portal.",
      "type": "shell",
      "command": "(make local-status >/dev/null 2>&1 || make local) && make local-portal",
      "presentation": {
        "reveal": "always",
        "panel": "dedicated",
        "focus": true
      },
      "problemMatcher": [],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
