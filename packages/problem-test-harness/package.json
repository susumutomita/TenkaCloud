{
  "name": "@tenkacloud/problem-test",
  "version": "0.1.0",
  "description": "Deterministic, offline local problem test harness for TenkaCloud problem packs (#2107). Validates author-declared contracts and runs scoring/probe logic with injected fakes — never AWS / GCP / Azure / Sakura / Docker / shell / network / credentials. Builds on @tenkacloud/problem-sdk as the single validation source.",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "bin": {
    "tenkacloud-pack-test": "bin/pack-test.ts"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage --coverage.reporter=lcov --coverage.reporter=text --coverage.reportsDirectory=./coverage --coverage.exclude='dist/**' --coverage.exclude='**/*.test.ts'"
  },
  "dependencies": {
    "@tenkacloud/problem-runtime": "workspace:*",
    "@tenkacloud/problem-sdk": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "fast-check": "^4.9.0",
    "typescript": "^6.0.3",
    "vitest": "^5.0.0"
  }
}
