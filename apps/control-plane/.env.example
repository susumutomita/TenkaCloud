# NextAuth.js Configuration
# Generate a secret: openssl rand -base64 32
AUTH_SECRET=
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:13000

# Auth0 Configuration
# Get these from your Auth0 Dashboard > Applications > Your App
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=
AUTH0_ISSUER=https://your-tenant.auth0.com

# Authentication Skip Mode (Development Only)
# Set to "1" to bypass Auth0 authentication with a mock session
# WARNING: Only use for local development, never in production
# AUTH_SKIP=1

# Application Plane URL (Local Development)
# Set this to redirect to local Application Plane
# NEXT_PUBLIC_APPLICATION_PLANE_URL=http://localhost:13001
