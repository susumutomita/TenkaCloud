# Keycloak Admin Console Credentials
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=change_this_password_in_production

# PostgreSQL Database Configuration
POSTGRES_DB=keycloak
POSTGRES_USER=keycloak
POSTGRES_PASSWORD=change_this_password_in_production

# Keycloak Hostname Configuration
KC_HOSTNAME=localhost
KEYCLOAK_PORT=8080

# NextAuth.js Configuration (for Control Plane UI)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=generate_random_secret_32_chars_minimum

# Keycloak OIDC Provider Configuration (for NextAuth.js)
KEYCLOAK_CLIENT_ID=control-plane-ui
KEYCLOAK_CLIENT_SECRET=generate_client_secret_in_keycloak
KEYCLOAK_ISSUER=http://localhost:8080/realms/TenkaCloud
