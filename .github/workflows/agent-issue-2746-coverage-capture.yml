name: Agent issue 2746 coverage capture

permissions:
  contents: write

on:
  push:
    branches:
      - agent/issue-2746-sakura-hardening

jobs:
  capture:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Check out the branch
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          ref: agent/issue-2746-sakura-hardening
          fetch-depth: 0
          submodules: true

      - name: Set up Bun
        uses: oven-sh/setup-bun@735343b667d3e6f658f44d0eca948eb6282f2b76
        with:
          bun-version: "1.3.11"

      - name: Install locked dependencies
        run: make install_ci

      - name: Capture infrastructure coverage output
        run: |
          set +e
          bun run scripts/workspace/run-coverage.ts --shard infrastructure > /tmp/issue-2746.log 2>&1
          status=$?
          mkdir -p .agent
          {
            echo "exit_status=$status"
            echo
            tail -n 1200 /tmp/issue-2746.log
          } > .agent/issue-2746-coverage-full.log

      - name: Commit diagnostics without racing other jobs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f .agent/issue-2746-coverage-full.log
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "chore(agent): capture Sakura infrastructure coverage output"
          for attempt in 1 2 3; do
            git pull --rebase origin agent/issue-2746-sakura-hardening
            if git push origin HEAD:agent/issue-2746-sakura-hardening; then
              exit 0
            fi
          done
          exit 1
