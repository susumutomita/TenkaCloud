[
  {
    "name": "awards the flag when the FlagValue output is present",
    "metadata": {
      "id": "golden-basic-find-the-flag",
      "runtime": { "provider": "aws", "engine": "cloudformation", "entry": "template.yaml" },
      "scoring": { "kind": "flag", "flagOutputKey": "FlagValue", "points": 100 }
    },
    "runtime": { "provider": "aws", "engine": "cloudformation", "entry": "template.yaml" },
    "deployment": "ready",
    "outputs": { "FlagValue": "TENKA{golden-reference-flag}" },
    "expected": { "valid": true, "score": "success" }
  },
  {
    "name": "is not runnable when the FlagValue output is missing",
    "metadata": {
      "id": "golden-basic-find-the-flag",
      "runtime": { "provider": "aws", "engine": "cloudformation", "entry": "template.yaml" },
      "scoring": { "kind": "flag", "flagOutputKey": "FlagValue", "points": 100 }
    },
    "runtime": { "provider": "aws", "engine": "cloudformation", "entry": "template.yaml" },
    "deployment": "ready",
    "outputs": {},
    "expected": {
      "valid": true,
      "score": "not-runnable",
      "diagnostics": ["SCORING_MISSING_OUTPUT_KEY"]
    }
  }
]
