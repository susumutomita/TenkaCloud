<!-- Generated by scripts/release/generate-release-report.ts. Do not edit. -->

# TenkaCloud release candidate 1.2.0-candidate.20260810

> Candidate / unverified: the launcher's default platform/catalog pair is immutable, but it is not certified for any mode or region.

The machine-readable source of truth is [`tenkacloud-release.json`](./tenkacloud-release.json), validated against [`tenkacloud-release.schema.json`](./tenkacloud-release.schema.json). Tags are display labels; the full commits and image digest below are authoritative.

> Publication gate: schema and semantic validation alone do not authenticate downloaded evidence bytes. `release:check` rejects `certified` until #2982 verifies every referenced artifact's schema and SHA-256 digest.

## Immutable release identity

| Component | Authoritative reference | Display tag |
| --- | --- | --- |
| Platform | [`6924122b4894ec7616aa241d2d729ddc27c891e7`](https://github.com/susumutomita/TenkaCloud/commit/6924122b4894ec7616aa241d2d729ddc27c891e7) | — |
| Problem catalog | [`9d308e3a14c75aa804216742dd2a9a31b7e824d1`](https://github.com/susumutomita/TenkaCloudChallenge/commit/9d308e3a14c75aa804216742dd2a9a31b7e824d1) | — |
| Simulator | `ghcr.io/susumutomita/tenkacloud-simulator@sha256:049c6c165f9947b386b2c5864983aebefba26e996ec62859dae0e9814c52d505` | — |

## Toolchain

| Component | Version |
| --- | --- |
| Bun | `1.3.11` |
| Node.js (development) | `24` |
| Node.js (Lite launcher) | `22` |
| AWS CDK CLI | `2.1133.0` |
| AWS CDK library | `2.262.1` |

## Qualification targets

| Mode | Qualification regions (not a support claim) |
| --- | --- |
| lite | ap-northeast-1 |

These targets may collect partial or failed evidence while the release remains a candidate. Only the next section is a certification claim.

## Certified compatibility

No deployment modes or AWS regions are certified by this candidate manifest.

### Schema and migration contracts

Contract versions are named per subsystem; TenkaCloud does not claim a single global database schema version.

| Contract | Version |
| --- | --- |
| problem-pack-manifest | `1` |
| always-on-control-plane-d1-migration | `0004_competitor_accounts` |

## Golden Path verification

No Golden Path run is attached. Three consecutive runs bound to this exact BOM must pass with distinct fresh-environment evidence, immutable runner identity, and distinct digest-pinned Golden Path plus passed residual-scanner-v1 reports for each supported mode and region before certification.

## Known limitations

- No fresh-environment Golden Path run is attached, so this release is not certified for third-party production use.
- No deployment mode or AWS region is certified by this manifest.
- This candidate manifest and report are a packaging layer created after the pinned platform commit; they are not the published v1.2.0 GitHub Release or a source tag.
- The pinned platform commit predates this manifest and residual-scanner packaging, so every future qualification run must record a separate immutable verification-runner repository and commit.
- The Lite launcher uses Node.js 22 because its managed CodeBuild image does not provide Node.js 24; repository development requires Node.js 24.
